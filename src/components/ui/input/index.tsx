import { ErrorSpan } from "./error-span";
import { InputLabel } from "./input-label";
import { InputRoot } from "./input-root";

export const Input = {
  Root: InputRoot,
  Label: InputLabel,
  Error: ErrorSpan,
};
